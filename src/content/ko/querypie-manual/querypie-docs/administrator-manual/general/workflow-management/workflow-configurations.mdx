import { Callout } from 'nextra/components'

## Overview
Workflow Configurations에서는 다음과 같은 결재 상신 및 승인 관련 설정을 변경합니다.

<Callout type="info">
10.2.6 버전부터 Slack DM 알림 관련 설정 위치가 Integrations 으로 이동하였습니다. 사용법은 [Slack DM 연동](Slack DM 연동) 문서를 참고해주세요. 
10.2.5 이하 버전에서의 Slack DM 설정 방법은 [10.1.0 버전 매뉴얼 문서](https://docs.querypie.com/ko/querypie-manual/10.1.0/workflow-configurations) 를 참고해주세요. 
</Callout>

## 결재 상신 및 승인 설정하기
Approval Rules 페이지 우측 상단에서 ⚙️ 버튼을 클릭하면 Workflow Configurations 모달이 팝업됩니다.

<p align="center">
<div>[스크린샷 2025-03-10 오전 11.09.49.png]()</div>
*Administrator &gt; General &gt; Workflow Management &gt; Approval Rules &gt; Workflow Configurations*
</p>

## 참조자 지정 활성화
Activate Review Step to collaborate with others 토글로 활성화합니다.

* Workflow 요청을 상신할 때 참조자를 지정하도록 허용할지 결정합니다.
* 토글을 켜면 요청 생성 화면의 결재 규칙 지정 영역에서 참조자 지정 버튼이 표시됩니다.
* 토글을 끄면 해당 버튼이 제거됩니다. (기존에 지정한 내역은 유지되며, 여전히 목록을 확인할 수 있습니다.)

## 사후 승인 허용
Allow users to submit requests in Urgent mode 토글로 활성화합니다.

* Workflow 요청을 상신할 때 사후 승인 모드(Urgent Mode)를 허용할지 결정합니다.
* 토글을 켜면 다음의 영역에서 사후 승인 관련 토글이 표시됩니다.Approval Rules : 결재 규칙 생성 및 수정Submit Request : 요청 생성 화면의 결재 규칙 지정 영역
  * Approval Rules : 결재 규칙 생성 및 수정
  * Submit Request : 요청 생성 화면의 결재 규칙 지정 영역
* 토글을 끄면 위 영역에서 사후 승인 관련 토글이 더 이상 표시되지 않습니다. 단, 기존에 이미 사후 승인 모드로 요청한 내역은 그대로 남아 있으며, 실행 가능합니다.

## SQL 요청 실행 시 쓰로틀링 활성화 (Use throttling when submitting SQL Request in Workflow)
토글을 켜면 Workflow에서 상신된 SQL 요청 실행시 쓰로틀링을 적용합니다.

이 옵션을 활성화하는 경우, 명시된 갯수만큼 쿼리를 연속적으로 실행한 뒤, 명시된 간격(ms)만큼 대기합니다. 이를 통해 대용량 쿼리 실행에 따르는 DB 부하를 방지할 수 있습니다.

* Maximum concurrent queries : 연속 실행 가능한 쿼리의 최대 개수 (1 ~ 999,999)
* Query throttle interval : 연속 쿼리 실행 후, 다음 연속 쿼리 실행 간의 간격 (1 ~ 9,999 ms)

## Workflow 양식에서 Tag로 필터링 (Tag Display and Filtering in DAC Workflow Requests)
Tag Display and Filtering in DAC Workflow Requests 토글 스위치로 활성화 합니다.

이 옵션을 활성화 하는 경우, 아래의 요청양식에서 태그로 대상을 필터링 할 수 있습니다.

* DB Access Request  : DB Connection에 부여된 태그를 목록에서 볼 수 있고 필터를 통해 특정 태그가 있는 커넥션만 필터링 할 수 있습니다.DB Access Reqeust 양식에서 태그 표시 및 필터링
